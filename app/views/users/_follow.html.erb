<%= form_for(current_user.active_relationships.build, remote: true) do |field| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= field.submit 'Follow', class: 'btn btn-primary' %>
<% end %>